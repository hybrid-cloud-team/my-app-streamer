# ECR 인증을 위한 Secret 생성 예시
# 
# 1. ECR 로그인 토큰 가져오기:
#    aws ecr get-login-password --region us-east-1
#
# 2. Secret 생성:
#    kubectl create secret docker-registry ecr-registry-secret \
#      --docker-server=YOUR_ECR_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com \
#      --docker-username=AWS \
#      --docker-password=$(aws ecr get-login-password --region us-east-1) \
#      --namespace=streamer
#
# 3. 또는 아래 YAML 파일 사용:
#    kubectl apply -f k8s/ecr-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: ecr-registry-secret
  namespace: streamer
type: kubernetes.io/dockerconfigjson
data:
  # 아래 값은 다음 명령어로 생성:
  # kubectl create secret docker-registry ecr-registry-secret \
  #   --docker-server=YOUR_ECR_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com \
  #   --docker-username=AWS \
  #   --docker-password=$(aws ecr get-login-password --region us-east-1) \
  #   --namespace=streamer --dry-run=client -o yaml
  .dockerconfigjson: "YOUR_BASE64_ENCODED_DOCKER_CONFIG_JSON"
