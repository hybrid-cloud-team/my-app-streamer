# 이 파일은 예시입니다. 실제 secret.yaml 파일을 생성하고 값을 채워넣으세요.
# kubectl create secret generic app-secret -n streamer \
#   --from-literal=db-password='YOUR_DB_PASSWORD' \
#   --from-literal=access-key='YOUR_AWS_ACCESS_KEY' \
#   --from-literal=secret-key-aws='YOUR_AWS_SECRET_KEY'
#
# 주의: SECRET_KEY는 설정하지 않으면 app.py의 기본값이 사용됩니다 (보안상 권장하지 않음)

apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: streamer
type: Opaque
stringData:
  db-password: "YOUR_DB_PASSWORD"        # TODO: 실제 DB 비밀번호로 변경
  access-key: "YOUR_AWS_ACCESS_KEY"      # TODO: 실제 AWS Access Key로 변경
  secret-key-aws: "YOUR_AWS_SECRET_KEY"  # TODO: 실제 AWS Secret Key로 변경
